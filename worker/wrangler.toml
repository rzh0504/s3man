name = "s3man-proxy"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# Custom domain (uncomment and set your domain):
# routes = [
#   { pattern = "files.yourdomain.com", custom_domain = true }
# ]

# Only one secret needed: wrangler secret put AUTH_TOKEN
# S3 credentials are passed per-request from the app (no provider env vars needed)
